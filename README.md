# Cash-Flow-Minimizer
Cash Flow Minimizer is a C++ console-based application that implements a greedy algorithm to reduce the number of financial transactions among a group of participants who have shared expenses.  The system calculates each participant’s net balance and intelligently determines the minimum number of payments required to settle all debts.
